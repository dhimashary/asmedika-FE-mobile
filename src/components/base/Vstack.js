import { View } from 'react-native'

export default function Vstack({ children }) {
  return (
    <View
      style={{
        flex: 1,
        alignItems: 'center',
        width: '100%',
      }}></View>
  )
}
